---
status: accepted
---

# 资源附件可以保存服务器资源包但不复制模组代码

可取得且未超过管理员配置上限的服务器资源包可以按内容哈希作为可选资源附件保存，并只在隔离回放会话中启用；播放端已经具有相同内容时直接复用，本地兼容模组仍是模型、纹理、声音与渲染器的首选来源。档案不复制模组 JAR，也不批量抽取模组资产，以避免任意代码分发、许可证、体积与版本冲突；资源附件缺失或失效时只能局部占位和诊断，不能阻止核心播放。
