---
status: accepted
---

# 默认不启用录制

DreamingRecall 安装后默认不开始录制：单人玩家必须主动开启，专用服务器则由具备权限的管理员通过指令启动或配置自动录制；专用服务器开始录制时默认向在线玩家告知，但管理员可以配置关闭。启用后仍使用持续分段、崩溃恢复、轮换与配额保留机制；选择 opt-in 默认值是为了避免未经明确选择的隐私采集和磁盘占用，同时不限制服主部署常驻录制。
