---
status: accepted
---

# 服务端落盘，管理员手动复制后由客户端播放

专用服务器只负责把源回放档案可靠地保存到世界目录。管理员通过 SSH、SFTP、面板或其他已有文件渠道把完整档案复制到客户端的 `dreamingrecall/replays` 目录，再由客户端本地读取；DreamingRecall 不实现远程流式读取、下载协议或普通玩家的档案访问。隔离回放世界与导演界面均运行在客户端，真实服务器不承载回放世界。单人回放通过同一读取边界直接使用本地档案。
