---
status: superseded by ADR-0016
---

# 通过可选适配器提供时间正确的远景

DreamingRecall 不把 Distant Horizons 或 Voxy 的私有数据库作为档案事实，而由可选远景适配器从可移植区块记录生成按档案、维度和播放时间上下文隔离的派生缓存；正常向前播放时增量更新，拖动时间轴时暂停远景摄取，精确落点后再异步切换或重建，以避免显示未来或旧地形并防止远景工作阻塞跳转。Distant Horizons 使用其公开路径与渲染 API，Voxy 社区版通过可替换软依赖适配器接入；任何适配器故障最多禁用对应远景，不得终止核心回放。
