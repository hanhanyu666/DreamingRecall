---
status: accepted
---

# 播放故障按最小可恢复范围隔离

单个对象、区块、提交分段或扩展损坏时，播放器优先占位、跳过或形成带诊断的局部缺口；索引损坏时从有效分段扫描重建，扩展轨道或适配器反复失败时只在本次隔离回放会话中停用该扩展。只有不存在任何有效起点或核心结构无法判定边界时才拒绝整份档案；此保证覆盖 DreamingRecall 控制的解码与扩展调用边界，但不虚假承诺能够恢复任意全局 Mixin、原生库或图形驱动造成的进程级崩溃。
