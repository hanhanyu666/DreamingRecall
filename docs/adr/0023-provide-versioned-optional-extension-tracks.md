---
status: accepted
---

# MVP 提供版本化的可选扩展轨道

为支持模组主动提高回放保真度，MVP 即提供最小公共扩展轨道 API：每个扩展声明稳定身份、模式版本、数据优先级、大小约束及兼容解码边界，缺少或不兼容的扩展可以跳过而不影响核心状态。DreamingRecall 不以保存任意未知自定义网络包替代扩展协议，因为原始包既无法保证跨模组版本解码，也可能包含与回放无关或敏感的数据。
