---
status: accepted
---

# 使用去重的空间化检查点与时间增量

随机访问不依赖从会话开头重放，也不按固定周期复制整个世界；档案以内容身份复用未改变的区块分区，通过状态检查点引用当时的世界、实体与全局状态，并以状态增量记录检查点之间的变化。初始以约 30 秒的检查点距离作为调优基线，再按局部变化量和落点延迟自适应，使播放器只读取镜头工作集并让长档案的跳转成本不随总时长线性增长；该结构与每 10 秒形成的崩溃恢复提交分段彼此独立。
