---
status: accepted
---

# 通过追加提交分段限制崩溃损失

回放档案以带完整性校验和完成标记的追加分段形成耐久边界，默认至多每 10 秒完成一次持久化提交；正常关闭时排空记录队列，异常终止或断电后则扫描至最后一个完整分段并舍弃未提交尾段，使此前档案仍可播放且最坏只损失约 10 秒。提交间隔允许管理员调整，但不以每 tick 强制刷盘换取零损失，因为这会违背服务器优先的录制性能约束；提交分段也不要求包含完整世界快照。
