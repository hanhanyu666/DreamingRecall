---
status: accepted
---

# 记录器过载时优先保护真实服务器

录制管线使用有界缓冲，服务端 tick 线程不得等待磁盘、压缩或归档工作；后台处理跟不上时，记录器先舍弃允许降级的增强数据，核心状态仍无法持续保存时则结束连续记录并明确标注录制缺口，而不是阻塞服务器、无限占用内存或静默丢失核心事件。资源恢复后必须从新的完整状态边界恢复录制，使缺口前后仍可独立播放并让管理员得到明确诊断。
