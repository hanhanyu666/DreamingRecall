---
status: accepted
---

# 同 Minecraft 版本内允许模组环境变化

回放只硬性要求播放端与录制端使用相同的 Minecraft 版本，不要求相同的模组清单或模组版本。档案因此必须同时提供不依赖数字注册表 ID 的可移植核心状态，以及仅在协议兼容时启用的原生增强数据；缺失或不兼容的模组内容以占位对象、跳过的非关键效果和明确诊断进行降级，不能阻止其余时间线播放。
