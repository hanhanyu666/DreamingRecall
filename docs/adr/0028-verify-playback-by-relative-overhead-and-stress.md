---
status: accepted
---

# 用相对帧开销和长时间随机访问验证播放性能

播放性能以相同场景普通旁观模式为基线，目标为 DreamingRecall 增加的 p95 客户端 CPU 帧耗时不超过约 2 毫秒，而不是对任意硬件、着色器和远景配置承诺固定 FPS；同时保持既定拖动与精确落点延迟。发布验收使用约 20 名玩家、两小时的全局档案进行连续播放与至少 10,000 次随机跳转，不得出现未处理崩溃，且内存占用必须随播放工作集而非档案总时长有界增长。
