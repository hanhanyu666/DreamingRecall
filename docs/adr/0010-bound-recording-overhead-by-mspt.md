---
status: accepted
---

# 用额外 MSPT 约束录制开销

DreamingRecall 以相同脚本负载下启用与关闭录制的 MSPT 差值验收录制性能，而不以可能已经受限的 TPS 是否变化判断；在约 20 名玩家跨维度探索并伴随实体活动的基准中，目标为 p95 额外 tick 耗时不超过 1 毫秒、p99 不超过 3 毫秒，且 tick 线程等待持久化的时间必须为零。后台 CPU、垃圾回收和磁盘争用计入该差值，持续超出预算时执行服务器优先的降级或录制缺口策略。
