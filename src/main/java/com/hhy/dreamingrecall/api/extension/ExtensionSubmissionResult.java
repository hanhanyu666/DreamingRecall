package com.hhy.dreamingrecall.api.extension;

public enum ExtensionSubmissionResult {
    ACCEPTED,
    DROPPED_BY_BACKPRESSURE,
    NOT_RECORDING,
    UNKNOWN_EXTENSION,
    PAYLOAD_TOO_LARGE,
    INVALID_CHANNEL
}
