# DreamingRecall

DreamingRecall 为 Minecraft 游戏会话提供可随机访问的回放。单人游戏与多人服务器属于同一个领域，只在运行形态上不同。

## Language

**回放会话**：
一段有明确起止时间的 Minecraft 世界活动记录，其来源可以是单人游戏的集成服务器，也可以是多人游戏的专用服务器。
_Avoid_: 录像、玩家录像

**回放档案**：
回放会话形成的源记录，保存其可观察状态、统一时间线及档案身份；会话封存后，导演的后期编辑不会改变它。
_Avoid_: 导演项目、视频文件、服务器备份

**回放数据源**：
隔离回放会话只读访问本机完整档案的入口；专用服务器档案由管理员通过已有文件管理方式完整复制到客户端后使用，单人档案则直接位于本机。DreamingRecall 不提供远程档案数据源。
_Avoid_: 真实服务器世界、回放运行位置、可写存档

**本地档案副本**：
由管理员从专用服务器手动取得并保留原档案身份的完整副本，可由 Minecraft 客户端脱离源服务器打开和播放。
_Avoid_: 导演项目、模组内下载、视频导出

**全局回放**：
一个 Minecraft 服务端进程内、一条统一时间线上的服务器权威记录，涵盖其所有维度、全部玩家、全局状态，以及回放会话期间曾达到完整可见状态的所有区块。录制期间从未加载的世界区域不属于回放。
_Avoid_: 全地图录像、全世界快照、旁观者录像

**回放覆盖范围**：
回放会话中曾进入完整可见状态的区块随时间形成的范围；导演只能可靠查看该时刻覆盖范围内的世界。
_Avoid_: 视距、整张地图

**区块观测区间**：
一个区块处于服务端完整加载且其变化被连续记录的时间区间；只有在该区间内，档案才承诺该区块状态对应指定档案时间。
_Avoid_: 区块存在时间、回放覆盖范围、玩家视距

**增量录制基线**：
录制启动后，在不暂停真实服务器的前提下分批建立的一致状态起点；对象完成基线与期间变化的校准后，才进入可靠观测范围。
_Avoid_: 暂停服务器快照、存档复制、瞬时全服快照

**未观测状态**：
区块离开观测区间后、再次进入前所呈现的最后已知地形，并明确携带时间不确定性；它不延续实体活动，也不能冒充该时刻的权威状态。
_Avoid_: 准确快照、缺失模组占位、录制缺口

**档案光照**：
区块观测期间由服务端提供并与档案时间对齐的天空光与方块光状态，是精确播放的首选光照来源；只有该状态不可用时，播放器才使用重算结果降级。
_Avoid_: 客户端渲染亮度、始终重算的光照、着色器效果

**单人回放**：
由单人游戏会话生成的全局回放，与多人服务器回放具有相同的能力基线，而不是功能受限的兼容模式。
_Avoid_: 本地录像、简化回放

**服务器重建视角**：
仅依据服务器权威记录还原的玩家观察位置与朝向；它不要求被观察玩家安装客户端组件，但不保证复现该玩家当时的精确镜头运动。
_Avoid_: 原始玩家视角、真实画面

**客户端镜头轨道**：
由参与录制的客户端提供、与全局时间线对齐的可选镜头状态记录，用于提高玩家视角的还原精度；它不包含世界副本、GUI 或屏幕像素。
_Avoid_: 高精度第一人称轨道、屏幕录像、客户端回放

**导演项目**：
引用一个回放档案的后期创作方案，包含导演制作的镜头路径、标记及时间编排，但不复制或改写源世界记录；同一回放档案可以关联多个导演项目。
_Avoid_: 回放档案、回放副本、视频文件

**镜头关键帧**：
导演在回放时间线上指定的一项镜头意图，定义该时刻虚拟镜头的位置与朝向；它是播放后的创作内容，不是录制时采集的玩家视角证据。
_Avoid_: 客户端镜头样本、玩家视角帧、录像帧

**导演镜头路径**：
由一组按时间排列的镜头关键帧形成的虚拟镜头运动，用于在回放世界中编排观察路线；它独立于客户端镜头轨道，可以在同一回放上反复编辑。
_Avoid_: 客户端镜头轨道、玩家移动轨迹、录制轨迹

**档案时间**：
从回放会话开始起单调递增的真实经过时间，是源记录与默认一倍速播放所使用的时间轴；服务器卡顿占用的真实时长仍属于档案时间。
_Avoid_: 系统时钟、服务器刻时间、固定二十刻时间

**服务器刻序号**：
可观察状态所属的服务端逻辑刻身份，用于保持状态分组和权威先后关系；它不把卡顿期间的真实时长压缩成一个正常刻。
_Avoid_: 档案时间、播放时间码、固定帧号

**瞬时事件**：
只在时间线正常向前经过发生时刻时呈现、且不构成持续世界状态的声音、粒子等记录；跳转到其后方不会补播被跨过的事件。
_Avoid_: 持久状态、跳转积压、状态检查点

**源服务器卡顿**：
回放会话产生时已经发生的掉刻或逻辑停顿，是档案时间中的历史现象；播放器可以流畅绘制它，但不能把它误报为播放性能问题。
_Avoid_: 播放器掉帧、解码卡顿、回放故障

**播放引擎卡顿**：
播放端因读取、解码、状态重建或资源处理而额外产生的画面停顿或交互失去响应，不属于源记录，应被视为播放器性能缺陷。
_Avoid_: 源服务器卡顿、历史掉刻

**拖动预览**：
导演持续移动时间轴指针时，自动时间推进暂停，播放器以低延迟、可取消的状态重建呈现目标附近的真实三维世界；它可以暂缓非关键瞬时效果，不代表最终精确状态。
_Avoid_: 静态缩略图、精确落点、完整播放

**精确落点**：
时间轴指针固定后，播放器在指定档案时间完成的全量状态恢复；它取代拖动预览，成为后续播放与导演操作的权威起点。
_Avoid_: 拖动预览、近似跳转、缩略图定位

**状态检查点**：
档案时间上的一个随机访问起点，引用足以恢复该时刻世界、实体与全局状态的数据，并可复用此前未改变的内容；它不等同于完整世界副本或耐久提交边界。
_Avoid_: 完整世界快照、提交分段、视频关键帧

**状态增量**：
两个状态检查点之间按时间记录的可观察变化，用于从较早检查点推进到精确目标时刻。
_Avoid_: 完整快照、服务器逻辑重演、未提交尾段

**播放工作集**：
隔离回放会话中已经实体化、可供当前镜头立即观察的全局状态与局部世界对象集合；档案覆盖范围不因某一区域暂未进入播放工作集而缩小。
_Avoid_: 回放覆盖范围、全部档案状态、服务端加载区块

**远景适配器**：
DreamingRecall 与特定 LOD 模组之间的可选播放集成，将当前档案状态提供给远景系统并隔离其故障；它不是核心回放解码器。
_Avoid_: 回放核心、模组兼容层、内置 LOD 引擎

**远景派生缓存**：
由回放档案为特定远景适配器生成、按档案与维度隔离的可丢弃数据；它不是历史事实，缺失或失效时可以重建。
_Avoid_: 回放档案、真实服务器 LOD 数据库、世界快照

**连续远景**：
同一回放档案内跨越跳转与倒退持续显示的 LOD 地形；它可以采用该档案较晚或最终已知的地形以避免远景突然加载，但不能冒充近景的时间精确状态。
_Avoid_: 时间精确地形、跨档案缓存、回放覆盖范围

**隔离回放会话**：
由不可变回放档案创建、与产生档案的真实世界完全分离的播放环境；其中的跳转、倒退和状态变化不会影响真实服务器或单人存档。
_Avoid_: 服务器倒带、存档回滚、真实世界回放

**降级回放**：
当录制时存在的模组内容或协议在播放环境中缺失或不兼容时，仍保持时间线可用，并以带有原始身份信息的占位对象替代无法还原的内容。
_Avoid_: 强制兼容、完整还原、忽略错误

**资源附件**：
按内容身份关联到回放档案、用于提高视觉与听觉还原度的服务器资源包副本；它不是模组代码，缺失时不影响核心时间线播放。
_Avoid_: 模组 JAR、回放核心数据、客户端全局资源包

**扩展轨道**：
由模组通过版本化 DreamingRecall 接口提供的可选可观察数据流，具有稳定身份、模式版本与降级边界；它不能成为核心回放可用性的前置条件。
_Avoid_: 未知原始网络包、核心状态、模组内部数据库

**扩展隔离**：
播放期间某个扩展轨道或适配器反复失败后，仅在当前隔离回放会话中停止使用该扩展并回退到核心表示，不卸载模组或修改档案。
_Avoid_: 全局禁用模组、吞掉核心错误、删除扩展数据

**播放版本边界**：
回放会话要求录制端与播放端使用相同的 Minecraft 版本；模组的增加、删除或版本变化不构成拒绝播放的理由，而是触发局部降级回放。
_Avoid_: 相同模组包、模组版本锁定、跨 Minecraft 版本播放

**档案格式版本**：
独立于 DreamingRecall 模组版本和 Minecraft 版本的档案模式身份，用于选择兼容读取或迁移路径；它不以模组包指纹代替。
_Avoid_: Minecraft 版本、模组版本、协议数字注册表

**可观察状态**：
能够影响导演在回放中看见、听见或检查到的服务器权威世界与玩家状态。模组数据库、任务队列、缓存及其他不可观察内部状态不属于该概念。
_Avoid_: 完整服务器状态、存档备份、模组内部状态

**玩家语音**：
由语音聊天模组传输的玩家音频，与普通游戏声音属于不同的数据类别；它不进入 MVP 核心档案，未来只能由明确启用的专用扩展轨道承载。
_Avoid_: 游戏声音、聊天消息、未知自定义网络包

**玩家聊天记录**：
按接收者保存的标准聊天框呈现序列，包含玩家实际收到的公开消息、私聊、队伍消息、系统通知及命令反馈；它不包含未发送的输入文字或服务器控制台内容。
_Avoid_: 公开聊天轨道、控制台日志、键盘输入记录

**玩家身份记录**：
回放中用于关联同一玩家的 UUID、名称及皮肤身份信息；网络地址、认证令牌和其他连接秘密不属于该记录。
_Avoid_: 连接日志、账号凭据、IP 记录

**录制许可**：
开始生成回放会话前所需的明确启用选择；单人游戏由玩家给出，专用服务器由具备权限的管理员给出。没有录制许可时，安装 DreamingRecall 本身不会产生回放档案。
_Avoid_: 安装即录制、隐式录制

**录制告知**：
专用服务器开始生成回放会话时向在线玩家显示的默认通知；管理员可以通过配置关闭，但它与录制许可不是同一概念。
_Avoid_: 管理员授权、聊天记录、强制同意

**手动录制档案**：
由玩家或管理员明确启动的回放档案，不受自动录制轮换删除策略支配。
_Avoid_: 自动录制档案、临时缓冲、保护标记

**自动录制档案**：
由管理员预先配置的自动策略生成、受磁盘配额与轮换规则管理的回放档案；被明确保护后不再作为自动删除候选。
_Avoid_: 手动录制档案、无限保留档案、未提交尾段

**服务器优先录制**：
一种以真实服务器持续运行优先于回放完整性的录制承诺；持久化资源不足时，记录器可以降级或暂停，但不能反向阻塞服务器逻辑。
_Avoid_: 无损优先录制、同步落盘、无限缓冲

**录制开销**：
相同服务器负载下，启用记录器后可归因于状态采集及后台资源争用的额外服务端耗时；它以 MSPT 差值衡量，不能用已经掉刻时表面不变的 TPS 代替。
_Avoid_: 播放性能、后台线程耗时、TPS 是否变化

**录制缺口**：
因记录器过载而明确标识、缺少连续核心状态的一段档案时间；缺口不能被伪装成完整记录，其前后可通过新的完整状态边界分别保持可播放。
_Avoid_: 源服务器卡顿、静默丢包、损坏档案

**提交分段**：
回放档案中已经完整写入、通过完整性校验并形成耐久边界的一段连续记录；提交分段是恢复单位，不等同于世界状态快照。
_Avoid_: 世界快照、内存缓冲、未完成尾段

**未提交尾段**：
档案末尾尚未形成耐久边界的临时记录；异常终止后可以舍弃它，而不影响此前提交分段的有效性。
_Avoid_: 录制缺口、损坏档案、完整分段
